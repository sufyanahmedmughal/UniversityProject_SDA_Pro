# ADR-003: Database Strategy

## Status: Accepted

## Decision
- PostgreSQL for incidents, alerts, audit logs (relational, ACID)
- Redis for threat intel cache, sessions, rate limit counters

## Reasoning
- Incidents have relational structure and require ACID transactions
- Threat intel lookups are expensive — Redis TTL cache reduces API calls
- Redis sorted sets ideal for rate limiting counters